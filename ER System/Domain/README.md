# Domain layer

Plain business concepts belong here.

Use this layer for entities, enums, and value objects such as expense reports, expense lines, users, departments, signatures, report statuses, approval statuses, money/rates, and date ranges. Domain code should avoid SQL Server, registry, WinForms, Crystal Reports, and file-system dependencies.
