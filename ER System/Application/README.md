# Application layer

Application workflows belong here.

Use this layer for report filing, approval, return/cancel, login, user account, signature, export, and validation workflows. Application code should coordinate domain rules and infrastructure abstractions without depending on WinForms controls.
